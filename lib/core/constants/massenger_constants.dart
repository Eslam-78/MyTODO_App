enum MassengerType { success, error, warning, info }

class MassengerDuration {
  static const short = Duration(seconds: 2);
  static const medium = Duration(seconds: 4);
  static const long = Duration(seconds: 6);
}
